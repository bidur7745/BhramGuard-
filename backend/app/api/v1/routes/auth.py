from __future__ import annotations

from fastapi import APIRouter


router = APIRouter(prefix="/auth", tags=["auth"])


# Auth endpoints will be implemented after manual database migrations are created.
